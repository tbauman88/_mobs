/// <reference types="cypress" />

import 'cy-mobile-commands'

describe('Login Page', () => {
  beforeEach(() => {
    cy.visit('')
  })

  it('should start at the Welcome slide', () => {
    
  })

  it('should allow the user to slide through onboarding', () => {
    
  })

  it('should redirect the user to the home page after successfully logging in', () => {
    
  })

  it('should redirect the user to the home page when Continue is clicked', () => {
    
  })

  it('should redirect the user to the home page when SKIP is clicked', () => {
    
  })
})

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vehikl.growth',
  appName: 'growth-sessions',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;

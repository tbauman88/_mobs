/// <reference types="cypress" />

describe('Profile Page', () => {
  beforeEach(() => {
    cy.visit('/profile')
  })

  it('should show the users Name and Github Nickname', () => {

  })

  it('should redirect the user to their linked Github when View Profile is clicked', () => {
    
  })

  it('should take the device settings for default appearance', () => {
    
  })

  it('should toggle the UI to dark/light mode', () => {
    
  })
})

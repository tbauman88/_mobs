/// <reference types="cypress" />

describe('SessionDetail Page', () => {
  beforeEach(() => {
    cy.visit('/home')
  })

  it('should show correct context for that session', () => {
    
  })

  it('should let the user visit their connected MobTime link', () => {
    
  })

  it('should redirect the user back to the Home page when the BACK button is clicked', () => {
    
  })

  it('should list the attendees in a togglable list', () => {
    
  })

  it('should allow the user to go to an attendees Github if Vehikl employee', () => {
    
  })

  it('should let the user view previous posted comments', () => {
    
  });

  describe.skip('Logged In', () => {
    it('should allow the user to post comments', () => {
      
    })

    it('should allow the user to join the growth sessiion', () => {
      
    })

    it('should let the user edit the session', () => {
    
    })
  
    it('should let the user delete the session', () => {
      
    })
  })
})
